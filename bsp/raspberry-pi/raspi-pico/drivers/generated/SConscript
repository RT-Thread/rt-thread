from building import *

# get current directory
cwd = GetCurrentDir()

src = Split("""
bs2_default_padded_checksummed.S
""")

path = [
    cwd + '/pico_base'
]

group = DefineGroup('pico-SDK', src, depend = [], CPPPATH = path)

Return('group')
