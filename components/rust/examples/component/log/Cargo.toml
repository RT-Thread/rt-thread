[package]
name = "em_component_log"
version = "0.1.0"
edition = "2021"

[lib]
name = "em_component_log"
crate-type = ["rlib", "staticlib"]

[features]
default = []
enable-log = []

[dependencies]
rt-rust = { path = "../../../core" }
