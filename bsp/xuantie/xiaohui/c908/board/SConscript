# RT-Thread building script for component

from building import *

cwd     = GetCurrentDir()
src     = ['startup_shim.S', 'm_mode_init.c', 'board.c', 'drv_uart.c', 'drv_ramdisk.c']
CPPPATH = [cwd]

group = DefineGroup('Drivers', src, depend = [''], CPPPATH = CPPPATH)

Return('group')
